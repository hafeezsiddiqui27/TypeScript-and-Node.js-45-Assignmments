let friends: string[] = [`saim`, `affan`, `ahmed`, `musa`];
let message: string = "you are invited for dinner tomorrow ";
for (let i = 0; i < friends.length; i++) {
  console.log(message + friends[i]);
}
